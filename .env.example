# Server
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Auth: comma-separated Bearer keys
MCP_API_KEYS=dev-key-1

# Web tool safety
WEB_ALLOWED_DOMAINS=example.com,duckduckgo.com,html.duckduckgo.com
WEB_FETCH_TIMEOUT_S=12
WEB_MAX_BYTES=800000

# File tool safety (sandbox root)
FILES_BASE_DIR=./data
