# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
APP_NAME=mcp-tool-gateway
HOST=127.0.0.1
PORT=8000

# Public URL used in auth metadata (keep in sync with how you access the service)
PUBLIC_BASE_URL=http://127.0.0.1:8000

# -----------------------------------------------------------------------------
# MCP
# -----------------------------------------------------------------------------
MCP_NAME=mcp-tool-gateway
MCP_MOUNT_PATH=/mcp

# -----------------------------------------------------------------------------
# Auth (optional)
# -----------------------------------------------------------------------------
# If false, the MCP server starts without any auth configuration.
MCP_ENABLE_AUTH=false

# Comma-separated pre-shared keys allowed to call POST /auth/token
# Example: MCP_API_KEYS=dev-key-1,dev-key-2
MCP_API_KEYS=dev-key-1,dev-key-2

# JWT settings (used only if MCP_ENABLE_AUTH=true)
# IMPORTANT: Change the secret in any real deployment.
MCP_JWT_SECRET=change-me
# Issuer is expected to be a URL by MCP auth settings. Keep it valid.
MCP_JWT_ISSUER=http://127.0.0.1:8000
MCP_JWT_AUDIENCE=mcp-clients
MCP_JWT_ALGORITHM=HS256
MCP_JWT_TTL_SECONDS=3600

# Optional scopes required for MCP tool calls (space or comma separated)
# Example: MCP_REQUIRED_SCOPES=mcp:tools
MCP_REQUIRED_SCOPES=mcp:tools
