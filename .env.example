# --- Server ---
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Comma-separated API keys. Agents must send: Authorization: Bearer <key>
MCP_API_KEYS=dev-key-1,dev-key-2

# --- Web tool policy ---
WEB_ALLOWED_DOMAINS=example.com,duckduckgo.com,html.duckduckgo.com
WEB_FETCH_TIMEOUT_S=12
WEB_MAX_BYTES=800000

# --- File tool policy ---
# Only files under this directory can be read.
FILES_BASE_DIR=./data
